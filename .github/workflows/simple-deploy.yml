name: 🚀 ABC English Explorer - Simple Deploy

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      pages: write
      id-token: write
      
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4

    - name: 🧹 Clean and Setup
      run: |
        # Clean existing files except .git and .github
        find . -maxdepth 1 -not -name '.git*' -not -name '.' -not -name '..' -exec rm -rf {} + 2>/dev/null || true
        echo "Repository cleaned successfully"

    - name: 🏗️ Build ABC English Explorer
      run: |
        echo "Building ABC English Explorer..."
        
        # Create main HTML file
        cat > index.html << 'EOF'
        <!DOCTYPE html>
        <html lang="he" dir="rtl">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <meta name="description" content="ABC English Explorer - פלטפורמה חינמית לילדי ישראל">
            <meta name="theme-color" content="#4FC3F7">
            <link rel="manifest" href="manifest.json">
            <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🤖</text></svg>">
            <title>🤖 ABC English Explorer | פלטפורמה חינוכית לילדי ישראל</title>
            <style>
                * { margin: 0; padding: 0; box-sizing: border-box; }
                body { 
                    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
                    background: linear-gradient(135deg, #4FC3F7 0%, #29B6F6 100%);
                    min-height: 100vh; color: white; direction: rtl;
                }
                .container { max-width: 1200px; margin: 0 auto; padding: 2rem; }
                
                .header { 
                    background: rgba(255,255,255,0.1); backdrop
